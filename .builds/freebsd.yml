image: freebsd/latest
sources:
- https://git.sr.ht/~mcf/cproc
- git://c9x.me/qbe.git
packages:
- gmake
environment:
  PATH: $HOME/qbe/obj:$PATH
tasks:
- build: |
    gmake -C qbe
    cd cproc
    ./configure
    make all check bootstrap
